// Plantilla. Copiá este archivo a "config.js" (en la misma carpeta) y pegá
// ahí tu propia API key -- config.js está en .gitignore, así que nunca se
// commitea ni se sube a GitHub. Por eso "Chattr Lab" solo funciona corriendo
// el sitio en tu propia PC (python3 -m http.server), no desde la URL pública
// de GitHub Pages, donde este archivo simplemente no existe.
//
// Nunca reutilices una key que ya haya aparecido en un chat, un issue, o
// cualquier lugar público -- generá una nueva.

const GEMINI_API_KEY = "PASTE_YOUR_API_KEY_HERE";
const GEMINI_MODEL = "gemini-2.5-flash";
