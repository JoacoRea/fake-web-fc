// PEGÁ ACÁ TU PROPIA API KEY DE GEMINI (gratis, Google AI Studio).
//
// Esta key queda visible en el código fuente de la página — es inevitable en
// un sitio 100% estático sin servidor. Restringila en Google Cloud Console
// (Credentials -> tu key -> Application restrictions -> HTTP referrers ->
// agregar "https://joacorea.github.io/*") ANTES de dejarla acá. Ver
// generator/README.md para el paso a paso completo.
//
// Nunca reutilices una key que ya haya aparecido en un chat, un issue, o
// cualquier lugar público -- generá una nueva.

const GEMINI_API_KEY = "PASTE_YOUR_RESTRICTED_API_KEY_HERE";
const GEMINI_MODEL = "gemini-2.5-flash";
